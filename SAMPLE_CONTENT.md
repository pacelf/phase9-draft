# Sample content

The optional sample content lives in `sample-content/`.

It is included so project teams can see how theme configuration changes affect Markdown formatting, content blocks, landing page blocks, galleries, logos, tables, colours, and responsive layouts.

When the project website is ready for real content, remove:

- `sample-content/`
- the `Sample Content Styles` section from `_data/navigation.yml`
- any unused sample assets in `assets/images/`
